[project]
name = "sentinel-ops"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.135.2",
    "requests>=2.33.0",
    "uvicorn[standard]>=0.42.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.2",
]
